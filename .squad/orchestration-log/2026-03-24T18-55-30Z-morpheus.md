# Morpheus Re-review Pass — Issue #2 / PR #36

**Timestamp:** 2026-03-24T18:55:30Z

## Spawn Context

- **Agent:** Morpheus (Security & Performance)
- **Task:** Re-review PR #36 after Link revision
- **Requested by:** Ryan Graham
- **Mode:** Sync

## Mandate

Validate that PR #36 updates to `MessageEnvelope<TPayload>` and `ReplayResponsePayload` resolve the prior two critical blockers:
1. Sequence ownership (broker-only vs. client traffic)
2. Replay reset boundaries (generation marker for reconnect safety)

## Review Execution

- **Revision diff:** `b4efdab` (rejected) → `54c8fb2` (approved)
- **Key paths reviewed:**
  - `src\SquadScout.Contracts\Messages\MessageEnvelope.cs`
  - `src\SquadScout.Contracts\Messages\ReplayRequestPayload.cs`
  - `src\SquadScout.Contracts\Messages\ReplayResponsePayload.cs`
  - `tests\SquadScout.Broker.Tests\MessageEnvelopeContractTests.cs`
- **Validation:**
  - `dotnet build .\SquadScout.slnx -nologo` ✓
  - `dotnet test .\SquadScout.slnx -nologo --no-build` ✓

## Outcome

**APPROVED**

### Resolution of Prior Blockers

1. **Sequence ownership:** `Sequence` now reserved for broker-owned replayable broker→client frames; nullable `ClientSequence` added for client-authored traffic. Client sequences cannot accidentally enter replay domain.

2. **Replay reset boundary:** Broker-minted `Generation` field added to envelope and `ReplayResponsePayload`. Reconnect logic can now distinguish resumed state from ordered-state reset after broker/PTY restart.

### Wire-Shape Safety

- `SessionMessageSerializer` omits null optional sequencing fields, preventing control frames (heartbeats, acks) from advertising a shared replay domain.
- Cumulative acknowledgement remains single source of truth (top-level).
- Heartbeat payloads carry liveness metadata only (no duplicate ack state).

## Impact

- **Phase gate cleared:** Issue #2 replay-envelope contract is now merge-safe.
- **Phase 2 unblocked:** Orleans grain implementation can proceed without contract ambiguity.
- **Next step:** Link and team can proceed with PR #36 merge.

---

**Morpheus signature:** Reviewed, verified, and approved for merge.
